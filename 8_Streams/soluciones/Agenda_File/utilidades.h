#ifndef UTILIDADES_H
#define UTILIDADES_H

#include <string>
#include <cstdlib>
#include <vector>
#include <sstream>
using namespace std;
class Utilidades
{
	public:
		static void extraerInt(string, int &, int &, int &, string);
		static void split(vector<string> &, string, char);
	protected:
		
		
};

#endif
